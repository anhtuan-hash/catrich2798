Mở Terminal ngay trong thư mục dự án Brian đang kết nối GitHub/Vercel, bảo đảm file V12.24.0 Update Only nằm trong Downloads, rồi dán nguyên lệnh sau:

bash -lc 'set -Eeuo pipefail; test -d .git && test -f package.json || { echo "❌ Hãy mở Terminal tại thư mục dự án Brian chính."; exit 1; }; BRANCH="$(git branch --show-current)"; test -n "$BRANCH" || { echo "❌ Không xác định được nhánh Git hiện tại."; exit 1; }; shopt -s nullglob; FILES=("$HOME"/Downloads/brian-english-studio-v12.24.0-admin-center-option-1-update-only*.zip); ((${#FILES[@]})) || { echo "❌ Không tìm thấy V12.24.0 Update Only trong Downloads."; exit 1; }; ZIP="$(ls -t "${FILES[@]}" | head -n 1)"; TMP="$(mktemp -d)"; trap '\''rm -rf "$TMP"'\'' EXIT; echo "📦 Đang cài: $ZIP"; git add -A; git commit -m "Backup before V12.24.0" >/dev/null 2>&1 || true; git pull --rebase origin "$BRANCH"; unzip -q "$ZIP" -d "$TMP"; SRC="$TMP"; COUNT="$(find "$TMP" -mindepth 1 -maxdepth 1 | wc -l | tr -d " ")"; ROOT="$(find "$TMP" -mindepth 1 -maxdepth 1 -type d | head -n 1)"; [ "$COUNT" = "1" ] && [ -n "$ROOT" ] && SRC="$ROOT"; rsync -av --exclude=".git/" --exclude=".env" --exclude=".env.*" --exclude=".vercel/" --exclude="node_modules/" --exclude="dist/" --exclude="public/fonts/" --exclude="public/bes-fonts/" "$SRC/" ./; export NVM_DIR="$HOME/.nvm"; if [ -s "$NVM_DIR/nvm.sh" ]; then . "$NVM_DIR/nvm.sh"; nvm install 22 >/dev/null; nvm use 22 >/dev/null; fi; npm ci; npm run verify:v12.24.0; git add -A; git commit -m "Update Brian English Studio to V12.24.0" || true; git push origin "$BRANCH"; echo "✅ Đã cài V12.24.0 và đẩy lên GitHub. Vercel đang tự triển khai."'
