# Activity Preview fullscreen and HTML export

- `Activity Preview` can enter browser fullscreen through the Fullscreen API.
- When native fullscreen is blocked (for example by iframe permissions), the interface uses a viewport-filling fallback.
- `Download my work` downloads one self-contained `.html` file.
- The exported file can be opened directly in a browser and does not require extracting a ZIP archive.
