# V10.32 — Remove Activity Studio

- Removed Activity Studio from the application registry and Apps directory.
- Removed the Activity Studio tile from the homepage and quick pins.
- Removed its route handler, lazy-loaded React pages, embedded TextLab package and standalone assets.
- Removed its design profile, icon mapping and permission entry.
- Existing Reading Studio activity generation remains available because it uses the shared activity engine independently.
