Tải file brian-english-studio-v12.37.0-ai-cloud-governance-sync-update-only.zip vào Downloads, mở Terminal tại thư mục dự án Brian chính rồi dán:

bash -lc 'set -Eeuo pipefail; test -d .git && test -f package.json || { echo "❌ Hãy mở Terminal tại thư mục dự án Brian chính."; exit 1; }; BRANCH="$(git branch --show-current)"; test -n "$BRANCH" || { echo "❌ Không xác định được nhánh Git hiện tại."; exit 1; }; shopt -s nullglob; FILES=("$HOME"/Downloads/brian-english-studio-v12.37.0-ai-cloud-governance-sync-update-only*.zip); ((${#FILES[@]})) || { echo "❌ Không tìm thấy V12.37.0 Update Only trong Downloads."; exit 1; }; ZIP="$(ls -t "${FILES[@]}" | head -n 1)"; TMP="$(mktemp -d)"; trap "rm -rf \"$TMP\"" EXIT; echo "📦 Đang cài: $ZIP"; git add -A; git commit -m "Backup before V12.37.0" >/dev/null 2>&1 || true; git pull --rebase origin "$BRANCH"; unzip -q "$ZIP" -d "$TMP"; SRC="$TMP"; COUNT="$(find "$TMP" -mindepth 1 -maxdepth 1 | wc -l | tr -d " ")"; ROOT="$(find "$TMP" -mindepth 1 -maxdepth 1 -type d | head -n 1)"; [ "$COUNT" = "1" ] && [ -n "$ROOT" ] && SRC="$ROOT"; rsync -av --exclude=".git/" --exclude=".env" --exclude=".env.*" --exclude=".vercel/" --exclude="node_modules/" --exclude="dist/" --exclude="public/fonts/" --exclude="public/bes-fonts/" "$SRC/" ./; export NVM_DIR="$HOME/.nvm"; if [ -s "$NVM_DIR/nvm.sh" ]; then . "$NVM_DIR/nvm.sh"; nvm install 22 >/dev/null; nvm use 22 >/dev/null; fi; rm -rf dist node_modules; npm ci; npm run verify:v12.37.0; git add -A; git commit -m "Update Brian English Studio to V12.37.0" || true; git push origin "$BRANCH"; echo "✅ Đã cài V12.37.0. Tiếp theo chạy supabase/brian_v12_37_ai_governance_cloud.sql trong Supabase SQL Editor, chờ Vercel Ready rồi nhấn Command + Shift + R."'
