import React from 'react';

// Route, account, AI, appearance, menu and notification status now live in GlobalFlatNavigation.
// Keep this compatibility component so older imports continue to build safely.
export default function StatusMenuBar() {
  return null;
}
