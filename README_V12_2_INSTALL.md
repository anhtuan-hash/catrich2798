# Install V12.2.0

Use Node.js 22.x.

```bash
npm ci
npm run verify:v12.2.0
npm run dev
```

This is a full-source release. Preserve your existing `.env` files and personal font assets when replacing an existing deployment.
