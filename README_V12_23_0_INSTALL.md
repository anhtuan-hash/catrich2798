# Brian English Studio V12.23.0

## Department Detail Workspace & Notification Bridge

- Removes the remaining 1180px width rail from Department detail modules.
- Keeps module tabs and AI/document assistance visible while the module body scrolls.
- Unifies Work Schedule detail cards with the V12.22 color-coded dashboard language.
- Mounts the Activity Center in the restored V11 shell so the notification button opens again.
- Keeps the detailed notification drawer available from the Activity Center.
- No SQL migration is required.

Run `npm run verify:v12.23.0` before deployment.
