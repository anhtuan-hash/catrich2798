# Brian English Studio V12.27.0 — Worksheet Keyword Source AI

Worksheet Factory Source & Input now supports **Từ khóa → AI**. Enter keywords or ideas, choose content type, length, tone and output language, then generate editable source content with the account's configured AI provider. The generated source is not silently replaced by a local draft when the provider fails.

No SQL migration is required.
