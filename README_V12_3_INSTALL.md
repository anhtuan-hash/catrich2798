# V12.3.0 Installation

This archive is a complete source tree. Use Node.js 22.

```bash
npm ci
npm run verify:v12.3.0
npm run dev
```

When replacing an existing project, preserve `.git`, `.env*` and personal font files.
