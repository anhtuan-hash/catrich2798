# V10.9 — Equal Tile App Drawer Layout

## What changed

- Redesigned `/apps` as a clean app drawer instead of a collage layout.
- All app tiles now share the same size and height for a more stable visual rhythm.
- Removed mixed large/wide/tall tile spans on the Apps page.
- Replaced the blue app grid well with a soft lavender / ivory / mint background.
- Kept each application accent color, icon, bilingual VI/EN copy, and Flat Window Launch Transition.
- Improved responsive layout: desktop 4–5 columns, tablet 2–3 columns, mobile 1 column.
- Ensured the page scrolls normally so every app can be reached.

## Verification

- `npm run build` passed.
- `npm test` passed: 22/22 smoke checks.
