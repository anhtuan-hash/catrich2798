# Stage 4 concurrent-main note

During Stage 4 implementation, `main` advanced after the branch was created. The Stage 4 diff remains scoped to the Brian UI workflow layer, its documentation, public exports and the UI smoke-build infrastructure.

The pull request must be evaluated against the latest `main` merge state. Merge only when GitHub reports the PR mergeable and all required workflows pass on that PR state.
