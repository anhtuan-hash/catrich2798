name: Debug Mobile Hero Build

on:
  push:
    paths:
      - '.github/workflows/debug-mobile-hero-build.yml'
      - 'scripts/test-editorial-app-heroes.mjs'
      - 'vercel.json'
      - 'src/components/mobile/MobileHomeView.jsx'
      - 'src/components/MobileHeroAdminField.jsx'
      - 'api/homepage-mobile-hero-publish.js'

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node 22
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Run Vercel build contract
        run: node scripts/test-editorial-app-heroes.mjs && npm run build
