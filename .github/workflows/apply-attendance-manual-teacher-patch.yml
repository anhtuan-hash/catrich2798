name: Apply Attendance Manual Teacher Patch

on:
  push:
    branches:
      - feat/attendance-manual-teacher-add
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: feat/attendance-manual-teacher-add
          fetch-depth: 0

      - name: Apply focused patch
        run: python scripts/patch-attendance-manual-teacher.py

      - name: Commit focused patch
        run: |
          if git diff --quiet; then
            echo "No patch changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/components/GlobalAttendanceNavigationTab.jsx
          git commit -m "feat: add manual teachers to attendance classes"
          git push origin HEAD:feat/attendance-manual-teacher-add
