import fs from 'node:fs';
const checks=[['legacy quarantine',fs.existsSync('src/styles/legacy/index.css')],['single legacy import',fs.readFileSync('src/main.jsx','utf8').includes("./styles/legacy/index.css")],['no direct v11 css imports',!fs.readFileSync('src/main.jsx','utf8').includes("./styles/v11")],['native textarea',fs.readFileSync('src/ui-core/primitives.jsx','utf8').includes('UITextArea')],['native dialog',fs.readFileSync('src/ui-core/primitives.jsx','utf8').includes('UIDialog')],['version',JSON.parse(fs.readFileSync('package.json','utf8')).version==='12.1.0']];let failed=0;checks.forEach(([n,ok],i)=>{console.log(`${String(i+1).padStart(2,'0')}. ${ok?'PASS':'FAIL'} — ${n}`);if(!ok)failed++});if(failed)process.exit(1);console.log(`V12.1 UI Core migration verification PASS (${checks.length}/${checks.length}).`);
