===== NHÁNH HIỆN TẠI =====
brian-ai-core

===== CÁC EXPORT TRONG ADAPTER =====
8:export async function fetchWithAiTimeout(url, init, timeoutMs = DEFAULT_TIMEOUT_MS) {
35:export function resolveServerAiProvider(requested = '') {
40:export function getServerAiReadiness(requested = '') {
109:export async function callServerAI({

===== DANH SÁCH EXPORT ĐỌC BẰNG NODE =====
[
  "callServerAI",
  "fetchWithAiTimeout",
  "getServerAiReadiness",
  "resolveServerAiProvider"
]

===== BIẾN MÔI TRƯỜNG ADAPTER ĐANG ĐỌC =====
36:  const provider = String(process.env.AI_PROVIDER || requested || 'openai').trim().toLowerCase();
45:      configured: Boolean(process.env.GEMINI_API_KEY && process.env.GEMINI_MODEL),
46:      model: process.env.GEMINI_MODEL || '',
52:    configured: Boolean(process.env.OPENAI_API_KEY && process.env.OPENAI_MODEL),
53:    model: process.env.OPENAI_MODEL || '',
59:  const apiKey = String(process.env.OPENAI_API_KEY || '').trim();
60:  const model = String(process.env.OPENAI_MODEL || '').trim();
84:  const apiKey = String(process.env.GEMINI_API_KEY || '').trim();
85:  const model = String(process.env.GEMINI_MODEL || '').trim();
